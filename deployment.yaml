apiVersion: apps/v1
kind: Deployment
metadata:
  name: boxer
  namespace: klinik
spec:
  replicas: 1
  selector:
    matchLabels: 
      app: boxer
  template:
    metadata:apiVersion: apps/v1
kind: Deployment
metadata:
  name: boxer
  namespace: klinik
spec:
  replicas: 1
  selector:
    matchLabels: 
      app: boxer
  template:
    metadata:
      labels:
        app: boxer
    spec:
      containers:
      - name: boxer
        image: riqimaruq/boxer:1
        resources:
          limits:
            memory: 10Mi
        ports:
        - name: web
          containerPort: 80
      automountServiceAccountToken: false
      labels:
        app: klinik
    spec:
      containers:
      - name: klinik
        image: riqimaruq/boxer:1
        resources:
          limits:
            memory: 10Mi
        ports:
        - name: web
          containerPort: 80
      automountServiceAccountToken: false
